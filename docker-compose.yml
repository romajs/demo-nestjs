version: '3'
services:
  db:
    image: mysql
    command: --default-authentication-plugin=mysql_native_password
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: 123mudar
      MYSQL_USER: demo-nestjs
      MYSQL_PASSWORD: 123mudar
      MYSQL_DATABASE: demo-nestjs
    ports:
     - 3306:3306
